{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "tools/postpone_task.schema.json",
  "title": "postpone_task arguments",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id"],
  "properties": {
    "task_id": {"type": "integer", "minimum": 1},
    "new_due_at": {"type": ["string", "null"], "format": "date-time"},
    "new_remind_at": {"type": ["string", "null"], "format": "date-time"}
  }
}

